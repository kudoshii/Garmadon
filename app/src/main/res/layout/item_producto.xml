<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="8dp"
    app:strokeWidth="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="12dp">

        <!-- Imagen del Producto -->
        <ImageView
            android:id="@+id/iv_producto_imagen"
            android:layout_width="0dp"
            android:layout_height="220dp"
            android:scaleType="centerCrop"
            android:src="@drawable/bicicleta"
            android:contentDescription="Imagen del producto"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Overlay con gradiente para mejorar legibilidad -->
        <View
            android:id="@+id/gradient_overlay"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:background="@drawable/gradient_overlay"
            app:layout_constraintBottom_toBottomOf="@id/iv_producto_imagen"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Botones de Edición (Visible solo en Mis Anuncios) -->
        <ImageButton
            android:id="@+id/btn_editar_producto"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_margin="8dp"
            android:background="@drawable/rounded_button_bg"
            android:src="@drawable/ic_edit"
            android:contentDescription="Editar producto"
            android:visibility="gone"
            android:elevation="4dp"
            app:tint="@color/white"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btn_eliminar_producto"
            tools:visibility="visible"/>

        <ImageButton
            android:id="@+id/btn_eliminar_producto"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_margin="8dp"
            android:background="@drawable/rounded_button_bg_red"
            android:src="@drawable/ic_delete"
            android:contentDescription="Eliminar producto"
            android:visibility="gone"
            android:elevation="4dp"
            app:tint="@color/white"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:visibility="visible"/>

        <!-- Título del Producto -->
        <TextView
            android:id="@+id/tv_producto_titulo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="12dp"
            android:layout_marginEnd="16dp"
            android:text="Nombre del Producto"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/iv_producto_imagen" />

        <!-- Precio del Producto -->
        <TextView
            android:id="@+id/tv_producto_precio"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="$99.990"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="?attr/colorPrimary"
            app:layout_constraintStart_toStartOf="@id/tv_producto_titulo"
            app:layout_constraintTop_toBottomOf="@id/tv_producto_titulo" />

        <!-- Badge de condición (Nuevo/Usado) -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chip_condicion"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:text="Nuevo"
            android:textSize="12sp"
            app:chipBackgroundColor="?attr/colorSecondary"
            app:chipCornerRadius="8dp"
            app:chipMinHeight="28dp"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tv_producto_precio"
            app:layout_constraintBottom_toBottomOf="@id/tv_producto_precio"
            tools:visibility="visible"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>